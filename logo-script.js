"use strict";(function globalOpacityAnimation() {    var content = document.getElementById('content');    content.style.display = 'none';    content.style.opacity = '0';    var preloaderLogo = document.getElementById('preloader');    var gray = document.getElementById('gray');    var color = document.getElementById('color');    var fps = 50;    var step = 0;    (function animationLoading(elem,time) {        var cycle = time/fps;        var height = 116;        var d0 = height/cycle;        elem.style.zIndex = 1;        elem.style.display = 'inline';            var timer = setInterval(function () {                height -= d0;                elem.style.height = height + 'px';                cycle--;                if (cycle <= 0) {                    gray.style.display = 'none';                    clearInterval(timer);                }            }, time / fps);        return step++;    })(gray,1500);    (function movieLogoTop(elem,time) {        var top = 50;//%        setTimeout(function () {            var timer = setInterval(function () {                top--;                elem.style.top = top + '%';                if (parseInt(elem.style.top) === 8){                    clearInterval(timer);                }            },time/fps);        },1500);        return step++;    })(color,1000);    (function opacityContentLogo(time) {        var cycle = time / fps;        var opacityLogo = 1;        var opacityContent = 0;        var d0 = opacityLogo/cycle;        setTimeout(function () {            content.style.removeProperty('display');            var timer = setInterval(function () {                opacityLogo -= d0 * 1.5;                opacityContent += d0;                preloaderLogo.style.opacity = opacityLogo;                content.style.opacity = opacityContent;                cycle--;                if(opacityContent >= 1){                    clearInterval(timer);                    preloaderLogo.style.display = 'none';                }            },time / fps);        },2000);    })(1200);})();